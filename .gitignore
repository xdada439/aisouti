# ── 构建产物 ──
build/
app/build/
.gradle/
.kotlin/
app/.cxx/
*.apk
*.aab

# ── 敏感文件：签名密钥与本机路径，绝对不能进版本库 ──
keystore.properties
*.keystore
*.jks
local.properties

# ── IDE ──
.idea/
*.iml
.DS_Store

# ── Python 后端 ──
__pycache__/
*.pyc
.venv/
backend/data/chroma/
backend/data/uploads/

# ── 本地调试产物 ──
*.db-journal

# ── 运行时数据与本地产物：不进版本库 ──
# backend/data 是后台管理页面写出来的用户数据（含用户自填的 AI Key），
# 代码对"文件不存在"有完整兜底，克隆后直接跑得起来，不需要模板文件。
backend/data/
*.db
*.tar.gz

# 历史工作记录，内含已废弃代码的引用
EXECUTION_REPORT.md
